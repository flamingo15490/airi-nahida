# 工作交接记录

## 交接用途
这个文件用于给后续 Codex / 开发者快速理解：
- 目前做到哪一步
- 哪些阶段已经基本落地
- 当前剩余问题主要在哪
- 下一步最合理做什么

## 当前任务背景
当前这条 `airi-nahida` 定制路线，已经不再停留在“第一阶段源码补丁迁移”。

截至目前，前七阶段的主体已经基本落地：
- 第一阶段：源码稳定化
- 第二阶段：本地协同稳定化
- 第三阶段：纳西妲人格层
- 第四阶段：外部记忆闭环与受控写回
- 第五阶段：受控主动陪伴治理层
- 第六阶段：协同判断层
- 第七阶段：系统健康与日用运维收口

当前更接近“阶段收口 + 验收归档 + 下一阶段规划”。

## 当前已确认的整体状态

### 1. 源码稳定化基础已基本完成
已确认：
- context bridge 净化修复已落地
- vision 已改成 App 级常驻运行
- stage-pages / stage-tamagotchi 的关键 typecheck 门槛已在 PR 验收快照中通过

### 2. 本地协同边界已建立
已确认：
- 外部集成设置层已建立
- legacy 配置与用户数据复用链路已建立
- memory / sidecar 的关键配置不再只依赖 renderer localStorage

### 3. external-memory 已进入稳定深化状态
已确认：
- 外部记忆已能读取、补充上下文、受控写回
- 写回结果已有统一 decision 字段
- heuristics 已更保守，减少误写与重复写
- settings 页已有最小可观测增强

当前判断：
- external-memory 这条线可以视为“已通过，可并入主线”

### 4. Nahida persona 已进入结构清晰、可继续细修状态
已确认：
- 人格 supplement 已重组为固定四层：
  - Fact anchors
  - Expression style
  - Memory boundaries
  - Taboos
- 三档模式边界已收紧
- reminder / proactive tone 已并入同一份 Nahida supplement
- settings 页已补只读验收面，但没有扩 schema

当前判断：
- nahida-persona 这条线可以视为“已通过，可并入主线”

### 5. proactive-companion 治理层已完成落地，但仍有已知限制
已确认：
- 事件筛选、冷却、展示强度、历史解释能力、治理看板都已落地
- AIRI 侧已从“黑箱没反应”提升到“收到事件后能解释为什么没反应”

当前已知限制：
- 当前更大的问题不是治理层本身
- 而是 sidecar 真实事件样本不足，导致高频实体验证反馈慢

当前判断：
- proactive-companion 可以视为“阶段性通过，可并入主线，但附已知限制”

### 6. companion coordination 协同判断层已完成验收收口
已确认：
- 已建立桌面端专用的 companion coordination manager、Eventa invoke 契约与 renderer store
- 已建立统一的 coordination snapshot / reason / surface 口径
- dashboard 已可展示 memory / persona / proactive 三个 surface 的状态、原因、活动与覆盖范围
- 主聊天发送前已刷新 coordination snapshot，并追加 coordination supplement
- proactive 事件经过治理后，也会刷新同一份 coordination snapshot

当前已知限制：
- persona target context 仍需要 renderer 至少同步一次，之后由主进程缓存复用
- 如果 renderer 长时间不刷新，persona 命中可能仍基于最后一次已知值
- 这一依赖已收缩到 `NahidaPersonaManager.updateTargetContext()` 一处，不再散在 coordination manager 内部

当前判断：
- 第六阶段可以视为“已通过，可作为新的稳定协同基线”

### 7. system health 与日用运维收口已完成第一轮落地
已确认：
- 已建立统一的 system health shared contract、主进程 manager 与 renderer store
- 已新增 health 设置页与 settings 首页快速摘要入口
- App 级自动 startup diagnostics 已接入
- 启动 diagnostics 失败时，会显示顶部非阻塞提示条
- 已新增 `D:\虚拟C盘\bat\start-airi-full.bat` 统一启动脚本

当前判断：
- 第七阶段可以视为“已通过，可作为新的低维护日用基线”

## 当前 PR 验收状态

当前 GitHub 验收快照：
- PR: `flamingo15490/airi-nahida#1`
- 标题：`[Draft] AIRI custom source integration snapshot (2026-06-04)`

已确认通过的核心自动化门槛：
- `pnpm -F @proj-airi/stage-pages typecheck`
- `pnpm -F @proj-airi/stage-tamagotchi typecheck`
- `pnpm -F @proj-airi/stage-ui test:run -- src/stores/mods/api/context-bridge.sanitize.test.ts`

手工验收现状：
- external-memory：已有实体验收
- nahida-persona：已有实体验收
- proactive-companion：治理层已落地，但高频真实样本仍不足
- companion coordination：关键链路与定向测试已通过，可进入归档收尾
- system health：已完成自动化门槛与功能验收，可进入归档收尾

## 当前新增的辅助资产

### 1. 文档
当前根目录中已经存在并值得继续维护的说明文档包括：
- `已实现功能.md`
- `00-项目总目标.md`
- `01-当前架构现状.md`
- `02-个性化要求.md`
- `03-边界与禁区.md`
- `04-最终愿景与阶段路线图.md`
- `05-工作交接记录.md`

### 2. GitHub 验收入口
当前已建立个人仓库与 PR 验收快照：
- 仓库：`flamingo15490/airi-nahida`
- PR：`#1`

### 3. PR1 验收专用启动脚本
为避免污染原有源码版启动脚本，当前已新增旁路验收脚本：
- `D:\虚拟C盘\bat\start-stage-tamagotchi-pr1-accept.bat`
- `D:\虚拟C盘\bat\start-stage-tamagotchi-pr1-accept.ps1`
- `D:\虚拟C盘\bat\启动AIRI源码版服务（PR1验收）.bat`

说明：
- 原有脚本未被改写
- 旧脚本已做备份
- 新脚本明确指向 `D:\airi-source\airi-pr1-accept`

### 4. 日用统一启动脚本
当前已新增：
- `D:\虚拟C盘\bat\start-airi-full.bat`

说明：
- 该脚本会按顺序启动 AIRI 源码版与 proactive companion sidecar
- 现有旧脚本未被改写
- 这个脚本主要服务于“少手工点几次”的日用场景

## 当前最重要的原则
- 不要为了继续推进而推翻已经落地的结构
- 不要把“可外置的东西”急着硬内置
- 不要把主动陪伴的样本不足误判成 AIRI 治理层彻底失败
- 当前重点应从“继续无限扩实现”切到“收口、归档、再规划”

## 下一步最合理做什么
后续接手者建议按以下顺序继续：
1. 阅读 `AGENTS.md`
2. 阅读本目录下的说明文档
3. 阅读 `已实现功能.md`
4. 查看 PR `flamingo15490/airi-nahida#1`
5. 明确当前哪些线已经通过，哪些线附已知限制
6. 先确认第七阶段的 system health / 启动脚本 / 自动诊断状态
7. 基于现有状态制定下一阶段计划

## 当前最值得继续深化的方向
不是继续堆更多基础设施，而是：
1. 记忆写回判定质量升级
2. 纳西妲表达质量继续细修
3. 主动陪伴与记忆 / 人格的协同细腻化
4. 继续降低日常使用中的维护成本与配置成本
5. 继续提升 system health 与协同状态的可解释性

## 当前工作树状态说明
- 当前前七阶段源码状态已具备继续整理 commit / PR 快照的基础。
- 当前分支名仍不应直接当长期稳定标签。
- 下一步最合理动作不是继续横向扩实现，而是：
  1. 以当前通过验收的状态整理工作树
  2. 用清晰的 Conventional Commit 落第七阶段快照
  3. 再决定是否合并回新的验收基线分支
