# 工作交接记录

## 交接用途
这个文件用于给后续 Codex / 开发者快速理解：
- 目前做到哪一步
- 已改过哪些文件
- 上一次停在哪
- 下一步最合理做什么

## 当前任务背景
当前在做 AIRI 源码版第一阶段迁移。
目标不是继续加本地零散补丁，而是把关键修复逐步迁移进 `D:\airi-source\airi` 源码，形成更稳定的定制开发版基础。

## 已确认的关键方向
### 1. Context bridge 修复方向
已知需要在源码层处理上下文中不可安全桥接/不可 structuredClone 的对象。
这一方向已经开始迁移，不应轻易推翻。

### 2. Vision 修复方向
已确认视觉系统的问题是结构性问题：
- 原本驱动逻辑在 `devtools/vision.vue`
- 页面卸载会停止 ticker / stream
- 正确方向是把运行职责上移到 App 级共享 runner

## 已修改文件（上一阶段）
- `packages/stage-ui/src/stores/mods/api/context-bridge.ts`
- `packages/stage-ui/src/stores/mods/api/context-bridge.sanitize.test.ts`
- `packages/stage-ui/src/stores/modules/vision/store.ts`
- `apps/stage-tamagotchi/src/renderer/composables/use-vision-runner.ts`
- `apps/stage-tamagotchi/src/renderer/composables/use-vision-screen-capture.ts`
- `apps/stage-tamagotchi/src/renderer/pages/devtools/vision.vue`
- `apps/stage-tamagotchi/src/renderer/App.vue`
- `packages/stage-pages/src/pages/settings/modules/vision.vue`

## 截至当前的实现状态
### 已完成
- context bridge 净化逻辑已迁移进源码
- context bridge 相关测试文件已新增
- vision store 已加入 `autoRun`
- 已建立共享 `useVisionRunner`
- 已尝试让 `App.vue` 接管 vision 常驻运行
- 已尝试让 `devtools/vision.vue` 改为共享 runner 模式
- 已在正式设置页加入 `auto-run vision capture`

### 未完成
- 还没有完整完成最后一轮验证
- 还没有确认 stage-tamagotchi 最新 typecheck 是否完全通过
- 还没有做完整 dev 运行验证
- 还没有最终确认 vision 常驻行为在源码版中完全符合预期

## 上一次停下时的验证情况
- `packages/stage-pages` 的 typecheck 已通过
- `stage-tamagotchi` 的 typecheck 之前报过一些问题
- 上一位 Codex 已尝试继续修复，但在最后验证前中断
- context bridge 测试曾有通过记录，但伴随 Playwright browser/page closed 的测试环境异常，需要后续重新判断

## 建议的接手顺序
下一位接手者建议按以下顺序继续：
1. 阅读 `AGENTS.md`
2. 阅读本目录下的说明文档
3. 审计上述已修改文件
4. 先跑 typecheck
5. 修剩余编译问题
6. 再决定是否跑最小测试
7. 最后再考虑 dev 运行验证

## 当前最重要的原则
- 不要扩大范围
- 不要动现有外部体系
- 不要推翻已有合理方向
- 先收尾第一阶段，再谈第二阶段

## 后续接手标准指令（建议）
可让新的 Codex 先执行：
- 阅读：
  - `00-项目总目标.md`
  - `01-当前架构现状.md`
  - `02-个性化要求.md`
  - `03-边界与禁区.md`
  - `04-最终愿景与阶段路线图.md`
  - `05-工作交接记录.md`
  - `AGENTS.md`
- 然后输出：
  1. 对最终目标的理解
  2. 对当前阶段优先事项的理解
  3. 一个以“先审计与验证、再收尾修复”为核心的计划
